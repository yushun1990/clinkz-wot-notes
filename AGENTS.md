# Agent Guidance

本文件定义 AI 如何在 `clinkz-wot-notes` 中研究、撰写、校验和维护文章。

## 1. 核心目标

AI 的任务不是代替作者“生产内容”，而是帮助作者：

- 恢复 ClinkZ-WoT 的最新事实；
- 理解设计问题和技术取舍；
- 暴露作者尚未真正理解的部分；
- 将理解转化为准确、可读、可持续维护的文章；
- 保证文章不会把计划、讨论或旧设计写成当前实现事实。

文章写作必须同时服务于项目传播和作者学习。

## 2. 会话入口

开始任何实质性写作前，按顺序执行：

1. 阅读本仓库的 `AGENTS.md`。
2. 阅读本仓库的 `PROJECT_STATE.md`。
3. 阅读 `CONTENT_PLAN.md`、`EDITORIAL_GUIDE.md` 和 `SOURCE_POLICY.md`。
4. 直接访问主项目：
   `https://github.com/yushun1990/clinkz-wot`
5. 不要通过 GitHub 全局搜索猜测仓库位置，不要使用名称相近的其他仓库。
6. 在主项目中至少读取：
   - `AGENTS.md`
   - `PROJECT_STATE.md`
   - `PLAN.md`
7. 根据文章主题，继续读取最小但足够的正式规范、ADR、workspace 讨论、
   源码和测试。
8. 在声称“当前实现如何工作”之前，必须检查对应源码和测试。
9. 记录本次文章依据的主项目 commit。

主项目持续演进。不得仅依赖聊天历史、旧文章或模型记忆来描述其当前状态。

## 3. 权威层级

### 主项目事实

发生冲突时，按主项目自身的 artifact ownership 处理：

1. 源码和测试：实现事实；
2. `docs/`：正式规范和已接受决策；
3. `AGENTS.md`、治理文件、`PLAN.md`、`PROJECT_STATE.md`：各自负责的治理、
   路线和连续状态；
4. `workspace/`：问题、提案、调查和推理历史，不是最终规范；
5. Git 历史：变更和可恢复检查点。

不要把 `workspace/` 中尚未迁移的讨论写成已接受设计。

### 本仓库事实

- `AGENTS.md`：稳定的 AI 写作规则；
- `SOURCE_POLICY.md`：事实分类和引用规则；
- `EDITORIAL_GUIDE.md`：语言、结构和表达规范；
- `CONTENT_PLAN.md`：选题、系列和推荐顺序；
- `PROJECT_STATE.md`：当前写作连续状态；
- `articles/`：文章源稿；
- `drafts/`：尚未进入正式文章目录的探索稿；
- `templates/`：模板，不是文章事实来源。

## 4. 事实分类

每个重要技术陈述都应在内部归入以下类别之一：

- `IMPLEMENTED`：源码和测试已经证明；
- `ACCEPTED_DESIGN`：正式规范或 ADR 已接受，但实现可能尚未完成；
- `PLANNED`：已经进入 PLAN 或 work package，但仍受前置条件约束；
- `EXPLORATORY`：仍在 workspace 或讨论中；
- `HISTORICAL`：旧版本曾采用，当前已被替代或仅作为迁移输入；
- `INFERENCE`：根据多个证据推导出的解释，必须明确写成推断。

文章正文不必展示这些枚举，但必须使用准确措辞。例如：

- “当前代码已经实现……”；
- “v1 架构已经决定……”；
- “计划在 WP-300 中实现……”；
- “项目仍在讨论……”；
- “早期设计曾经……”；
- “从这些约束可以推断……”。

## 5. 写作流程

### 阶段 A：研究简报

在起草正文前先产出研究简报，包括：

- 文章要回答的核心问题；
- 当前主项目 commit；
- 相关正式文档；
- 相关源码和测试；
- 已实现、已接受、计划中和未决内容；
- 被否决或被替代的方案；
- 仍缺少的证据。

### 阶段 B：作者理解校验

写作前帮助作者用自己的话解释：

- 问题是什么；
- 为什么直觉方案不够；
- 当前选择解决了什么；
- 付出了什么代价。

发现理解缺口时，应先解释和追问，不应直接用流畅文案掩盖缺口。

### 阶段 C：提纲

提纲至少包含：

1. 现实问题；
2. 直觉方案；
3. 失败场景；
4. 替代方案；
5. ClinkZ-WoT 的选择；
6. Rust/模块/生命周期映射；
7. 当前边界与尚未完成内容；
8. 总结。

### 阶段 D：初稿

遵循 `EDITORIAL_GUIDE.md`。正文应先解决普遍问题，再介绍
ClinkZ-WoT 的具体选择，避免从项目内部术语直接起笔。

### 阶段 E：事实校验

依据 `SOURCE_POLICY.md` 检查：

- 是否把设计写成实现；
- 是否使用了过时文档；
- 是否遗漏版本基线；
- 是否误解源码；
- 是否把推断写成确定事实；
- 是否引用了正确的权威 owner；
- 是否需要标记后续可能变化的 API。

### 阶段 F：发布同步

发布后更新：

- Front Matter 中的 `published_at` 和 `canonical_url`；
- `CONTENT_PLAN.md` 中的状态；
- `PROJECT_STATE.md` 中的下一篇和未决事项；
- 前后文章导航；
- 必要时在旧文章中加入版本或修订提示。

## 6. 输出要求

AI 产出的正式文章必须：

- 使用中文正文，保留必要的英文技术术语；
- 给出清楚的标题和一句话摘要；
- 说明基于哪个 ClinkZ-WoT commit；
- 明确哪些内容尚未实现或仍可能变化；
- 尽量包含一个具体的数据流、状态机、代码接口或失败场景；
- 避免把架构宣传语当成论证；
- 避免虚构 benchmark、用户规模、性能结论和 API；
- 不编造引用、提交、文件路径或测试结果；
- 不依赖只有当前会话知道的信息。

## 7. 风格禁区

避免：

- “众所周知”“显而易见”“不难发现”等空泛过渡；
- 连续堆砌抽象名词；
- 没有失败场景的架构结论；
- 把所有方案写成“各有优劣”却不做判断；
- 为显得成熟而隐藏尚未完成的部分；
- 过度使用“赋能、生态、闭环、颠覆、最佳实践”等营销词；
- 生成看似合理但仓库中不存在的 Rust API。

## 8. 连续状态

完成一次有意义的研究、提纲、初稿、事实校验或发布后，更新
`PROJECT_STATE.md`。

检查点问题：

> 如果当前会话立即结束，一个没有聊天历史的新 AI 能否只靠仓库继续写作？

如果不能，先更新状态文件。
